{
  "name": "@dinstack/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env local --port 8000",
    "dev:web-app": "pnpm run dev",
    "dev:web-content": "pnpm run dev",
    "build": "tsc --build && tsc-alias",
    "preview": "wrangler dev --env local --port 8000",
    "typecheck": "tsc --noEmit",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy --env production"
  },
  "dependencies": {
    "@dinstack/db": "workspace:^",
    "@dinstack/shared": "workspace:^",
    "@trpc/server": "^10.44.1",
    "arctic": "^0.3.6",
    "lodash-es": "^4.17.21",
    "oslo": "^0.23.5",
    "superjson": "^2.2.1",
    "ts-expect": "^1.3.0",
    "ts-md5": "^1.3.1",
    "ts-pattern": "^5.0.6",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@dinstack/tsconfig": "workspace:^",
    "@types/lodash-es": "^4.17.12",
    "tsx": "^4.6.2"
  }
}
